<div class="form-group">
  <%= f.label :labels %>
  <%= f.text_field :labels,
                    class: 'form-control',
                    place: "Enter labels (separated by commas)",
                    value: labels.map(&:name).join(', ') %>
</div>
